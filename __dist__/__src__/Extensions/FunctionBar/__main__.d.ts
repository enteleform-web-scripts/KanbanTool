import { Module } from "~/Utils/Module_BaseClasses";
export declare class FunctionBar extends Module {
    static load(features: {
        CardLegend: boolean;
        ModeLegend: boolean;
    }): void;
}
